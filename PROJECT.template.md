# PROJECT.md

> Copy this file to your project root as `PROJECT.md` and fill it in. Every skill in the
> framework reads this file so the skills themselves can stay generic. Leave a field as
> `TBD` if you don't know it yet — skills will treat `TBD` as an open question to resolve,
> not a fact.

## 1. One-liner
<!-- What is this product, in one sentence? -->

## 2. The agent
- **Core task(s)** the agent performs:
- **Autonomy level**: <!-- retrieves/drafts only | takes real-world actions | mix -->
- **Real-world actions it can take** (if any): <!-- sends email, writes to DB, spends money, files tickets... -->
- **Where a wrong action is costly** (the failures you most want to prevent):
- **Acceptable to fail safely?** <!-- when unsure, should it stop and ask, or proceed? -->

## 3. Users & job-to-be-done
- **Primary user**:
- **What they're trying to get done**:
- **How they do it today** (the status quo you're replacing):

## 4. Stack & constraints
- **Platform**: <!-- web app | mobile | CLI | API | ... -->
- **Backend / language**:
- **Datastore(s)**:
- **Model(s)** the agent uses:
- **Tools / integrations** the agent calls:
- **Budget / cost ceiling** (per task or per month):
- **Latency expectations**:
- **Compliance / privacy constraints** (PII, regulated data, retention rules):

## 5. Success definition
- **What "the agent works" means concretely**:
- **Target task success rate**:
- **Unacceptable failure modes** (hard lines it must never cross):

## 6. Stage & scope
- **Current stage**: <!-- prototype (de-risking) | building MVP | iterating live -->
- **Hard out-of-scope for v1** (things you're deliberately NOT building yet):
- **Deadline / time budget**:

## 7. Anything else the skills should know
<!-- Quirks, prior decisions, opinions about format, links to existing docs. -->
